language: php
php: 
- 7.1.3

before_script:
- cp .env.travis env
- composer self-update
- composer install --no-interaction

script:
  - vendor/bin/phpunit
deploy:
provider: heroku
api_key:
  secure: KEY
app: martischool
on:
  repo: GeovannyMero/martischool
